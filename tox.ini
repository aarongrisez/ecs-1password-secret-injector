[tox]
envlist = py{39,310,311}
minversion = 4.3.5
isolated_build = true

[testenv]
deps =
    pytest
commands =
    pytest tests {posargs}

[testenv:lint]
skip_install = true
deps =
    ruff
commands =
    ruff check src tests
